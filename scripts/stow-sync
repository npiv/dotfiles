#!/usr/bin/env sh
set -eu

REPO_DIR="$(cd "$(dirname "$0")/.." && pwd)"
PACKAGES="fish nvim alacritty zellij tmux wezterm claude userbin codex"

cd "$REPO_DIR"

if [ "${1-}" = "--adopt" ]; then
  exec stow --adopt -v -t ~ $PACKAGES
fi

exec stow -v -t ~ $PACKAGES
