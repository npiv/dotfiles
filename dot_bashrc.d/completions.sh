# # Load custom bash completions
# if [[ -d "$HOME/.local/completions" ]]; then
#     for comp in "$HOME"/.local/completions/*; do
#         # Only source actual files
#         [[ -f "$comp" ]] && source "$comp"
#     done
# fi
