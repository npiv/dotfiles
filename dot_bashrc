export BASH_SILENCE_DEPRECATION_WARNING=1

export PATH="/opt/homebrew/bin:$PATH"
export PATH="~/bin/:$PATH"
export PATH="/usr/local/bin/:$PATH"

if command -v zoxide &> /dev/null; then
    eval "$(zoxide init bash)"
fi
if command -v starship &> /dev/null; then
    eval "$(starship init bash)"
fi
if command -v atuin &> /dev/null; then
    eval "$(atuin init bash --disable-up-arrow)"
fi

for f in ~/.bashrc.d/*; do
	. "$f" || true
done

if [ -f ~/.bashrc.local ]; then
	. ~/.bashrc.local
fi

export PATH="$HOME/.local/bin:$PATH"


# PAI Configuration (added by Kai Bundle installer)
export DA="Data"
export TIME_ZONE="Europe/Brussels"
export PAI_SOURCE_APP="$DA"
export ELEVENLABS_API_KEY="sk_f2716be4bd86cd9dec58b69e5fae645374893a32e9e48e78"
export ELEVENLABS_VOICE_ID="s3TPKV1kjDlVtZbl4Ksh"
